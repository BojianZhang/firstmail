# 复制本文件为 .env 并填入真实值：cp .env.example .env

# FirstMail 接口 API Key（必填）
FIRSTMAIL_API_KEY=your_api_key_here

# 监听端口（可选，默认 8787）
PORT=8787

# 监听地址（可选，默认 127.0.0.1）
# 用 Nginx 反代时保持 127.0.0.1 最安全；要让 Node 直接对外监听才改成 0.0.0.0
HOST=127.0.0.1
